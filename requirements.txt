# Tested with Python 3.12
fastapi>=0.111
uvicorn[standard]>=0.29
python-multipart>=0.0.9
langchain-core>=0.2
langgraph>=0.1
# langchain-huggingface no longer required — LLM invocation uses tokenizer.apply_chat_template directly
transformers>=4.51.0
torch>=2.0
accelerate>=0.30
kubernetes>=29.0
pyyaml>=6.0
lancedb>=0.8
tantivy>=0.22          # native FTS index for LanceDB keyword search
pyarrow>=14.0          # Arrow schema support (auto-installed by lancedb, listed explicitly)
pandas>=2.0            # Excel ingestion via ingest_excel()
openpyxl>=3.1          # pandas Excel engine (.xlsx read)
sentence-transformers>=3.0
pypdf>=4.0
markdown-it-py>=3.0
psutil>=5.9
httpx>=0.27
python-dotenv>=1.0
nvidia-ml-py>=12.0
